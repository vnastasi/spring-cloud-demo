spring:
  application:
    name: station-registry
  cloud:
    config:
      uri: http://localhost:8081
management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    git:
      enabled: true
      mode: full
info:
  app:
    name: Station registry
    version: ${project.version}
server:
  port: 8085